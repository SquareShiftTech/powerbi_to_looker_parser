"""Wires handlers; builds MetadataModel. Order: tables -> dimension -> measure -> calc_field."""

from typing import Any


def run(raw: dict[str, Any]) -> Any:
    """Build canonical MetadataModel from raw. Placeholder."""
    raise NotImplementedError("semantic orchestrator run")
