"""raw -> tables, relationships, datasource shell."""

from typing import Any


def run(raw: dict[str, Any]) -> tuple[Any, Any, Any]:
    """Placeholder. Returns (tables, relationships, datasource_shell)."""
    raise NotImplementedError("tables")
